user  nginx;
worker_processes  1;
events {
  worker_connections  1024;
}

http {
  include /etc/nginx/mime.types;
  server {
    listen 80;

    location /products {
        root /images;
    }

    location /merchants {
        root /images;
    }
  }
}